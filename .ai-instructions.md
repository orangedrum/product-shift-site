# ProductShift & User Mirror: AI Instructions & Guardrails

## 1. Documentation Index
This workspace uses a modular instruction system to prevent "Context Bloat" and ensure diff accuracy.

Refer to these project-specific instruction sets to avoid context loss:
1.  **Global Rules & Protocol:** `.ai-rules.md` (Design system, debugging, auth).
2.  **Project Alpha (The Brag Engine):** `.ai-instructions-alpha.md` (Resume builder).
3.  **Project Beta (Community Intelligence):** `.ai-instructions-beta.md` (Project Intelligence).

## 2. High-Level Strategy: From Guessing to Proving
We are integrating a robust infrastructure beneath ProductShift that captures continuous user sentiments.
- **Pilot User:** FreeBrain.
- **Core Objective:** Build data-backed Mirror Personas ready to test initiatives against.
- **Primary Pivot:** Moving from "Guessing" (Synthetic personas) to "Proving" (Community-backed data).

## 3. Project Beta MVP Plan (The Priority)
The Community Analyzer leverages the "Content Vault" chassis from the Resume Engine to build a data-shredding factory.

### Step 1: The Data Bedrock (Supabase)
- Define tables for `community_assets`, `community_personas`, `experiments`, and `experiment_sessions`.

### Step 2: The Ingestion Factory
- Adapt the `careerIngestHandler` into a `communityIngestHandler`.
- Update `prompts.ts` with the new Community Extraction logic.

### Step 3: The Proof Hub (Frontend)
- Refactor `PublicResume.tsx` into `PublicExperiment.tsx`.
- Implement the "Kickstarter Treatment" header and tabbed interface.

### Step 4: Persona Versioning
- Create the `persona_evolution` table and "Recalibrate" UI.

### Step 5: Growth Flywheel
- Build the GHL (GoHighLevel) JSON export for automated social growth.